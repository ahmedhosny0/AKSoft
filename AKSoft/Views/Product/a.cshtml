
<h2>Employee CRUD Operations</h2>

<a class="btn btn-success" style="margin-bottom:10px" onclick="PopupForm('@Url.Action("ItemLastPurchasePrice", "Reports")')"><i class="fa fa-plus"></i> Add New</a>

    <script>
        var Popup, dataTable;
       
        function PopupForm(url) {
            var formDiv = $('<div/>');
            $.get(url)
            .done(function (response) {
                formDiv.html(response);

                Popup = formDiv.dialog({
                    autoOpen: true,
                    resizable: false,
                    title: 'Item Last PurchasePrice',
                    height: 230,
                    width: 370,
                    close: function () {
                        Popup.dialog('destroy').remove()
                    }

                });
            });
        }
    </script>




